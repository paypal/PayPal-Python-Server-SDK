"""paypalserversdk.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from paypalserversdk.api_helper import APIHelper
from paypalserversdk.models.customer_information import (
    CustomerInformation,
)
from paypalserversdk.models.vault_instruction import (
    VaultInstruction,
)


class ApplePayAttributes(object):
    """Implementation of the 'Apple Pay Attributes' model.

    Additional attributes associated with apple pay.

    Attributes:
        customer (CustomerInformation): This object represents a merchant’s customer,
            allowing them to store contact details, and track all payments associated
            with the same customer.
        vault (VaultInstruction): Base vaulting specification. The object can be
            extended for specific use cases within each payment_source that supports
            vaulting.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "customer": "customer",
        "vault": "vault",
    }

    _optionals = [
        "customer",
        "vault",
    ]

    def __init__(
        self,
        customer=APIHelper.SKIP,
        vault=APIHelper.SKIP):
        """Initialize a ApplePayAttributes instance."""
        # Initialize members of the class
        if customer is not APIHelper.SKIP:
            self.customer = customer
        if vault is not APIHelper.SKIP:
            self.vault = vault

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        customer =\
            CustomerInformation.from_dictionary(
                dictionary.get("customer"))\
                if "customer" in dictionary.keys()\
                else APIHelper.SKIP
        vault =\
            VaultInstruction.from_dictionary(
                dictionary.get("vault"))\
                if "vault" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(customer,
                   vault)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _customer=(
            self.customer
            if hasattr(self, "customer")
            else None
        )
        _vault=(
            self.vault
            if hasattr(self, "vault")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"customer={_customer!r}, "
            f"vault={_vault!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _customer=(
            self.customer
            if hasattr(self, "customer")
            else None
        )
        _vault=(
            self.vault
            if hasattr(self, "vault")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"customer={_customer!s}, "
            f"vault={_vault!s}, "
            f")"
        )
