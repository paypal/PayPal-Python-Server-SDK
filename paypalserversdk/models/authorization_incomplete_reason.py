# -*- coding: utf-8 -*-

"""
paypalserversdk

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class AuthorizationIncompleteReason(object):

    """Implementation of the 'Authorization Incomplete Reason' enum.

    The reason why the authorized status is `PENDING`.

    Attributes:
        PENDING_REVIEW: Authorization is pending manual review.
        DECLINED_BY_RISK_FRAUD_FILTERS: Risk Filter set by the payee failed
            for the transaction.

    """
    PENDING_REVIEW = 'PENDING_REVIEW'

    DECLINED_BY_RISK_FRAUD_FILTERS = 'DECLINED_BY_RISK_FRAUD_FILTERS'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
