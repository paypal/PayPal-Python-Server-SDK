# -*- coding: utf-8 -*-

"""
paypalserversdk

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class PricingModel(object):

    """Implementation of the 'Pricing Model' enum.

    The pricing model for the billing cycle.

    Attributes:
        FIXED: A fixed pricing scheme where the customer is charged a fixed
            amount.
        VARIABLE: A variable pricing scheme where the customer is charged a
            variable amount.
        AUTO_RELOAD: A auto-reload pricing scheme where the customer is
            charged a fixed amount for reload.

    """
    FIXED = 'FIXED'

    VARIABLE = 'VARIABLE'

    AUTO_RELOAD = 'AUTO_RELOAD'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
