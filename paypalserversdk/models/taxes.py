# -*- coding: utf-8 -*-

"""
paypalserversdk

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from paypalserversdk.api_helper import APIHelper


class Taxes(object):

    """Implementation of the 'Taxes' model.

    The tax details.

    Attributes:
        percentage (str): The percentage, as a fixed-point, signed decimal
            number. For example, define a 19.99% interest rate as `19.99`.
        inclusive (bool): Indicates whether the tax was already included in
            the billing amount.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "percentage": 'percentage',
        "inclusive": 'inclusive'
    }

    _optionals = [
        'inclusive',
    ]

    def __init__(self,
                 percentage=None,
                 inclusive=True):
        """Constructor for the Taxes class"""

        # Initialize members of the class
        self.percentage = percentage 
        self.inclusive = inclusive 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        percentage = dictionary.get("percentage") if dictionary.get("percentage") else None
        inclusive = dictionary.get("inclusive") if dictionary.get("inclusive") else True
        # Return an object of this model
        return cls(percentage,
                   inclusive)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'percentage={self.percentage!r}, '
                f'inclusive={(self.inclusive if hasattr(self, "inclusive") else None)!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'percentage={self.percentage!s}, '
                f'inclusive={(self.inclusive if hasattr(self, "inclusive") else None)!s})')
