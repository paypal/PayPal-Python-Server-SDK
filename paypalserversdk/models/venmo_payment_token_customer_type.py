# -*- coding: utf-8 -*-

"""
paypalserversdk

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class VenmoPaymentTokenCustomerType(object):

    """Implementation of the 'Venmo Payment Token Customer Type' enum.

    The customer type associated with the Venmo payment token. This is to
    indicate whether the customer acting on the merchant / platform is either
    a business or a consumer.

    Attributes:
        CONSUMER: The customer vaulting the Venmo payment token is a consumer
            on the merchant / platform.
        BUSINESS: The customer vaulting the Venmo payment token is a business
            on merchant / platform.

    """
    CONSUMER = 'CONSUMER'

    BUSINESS = 'BUSINESS'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
